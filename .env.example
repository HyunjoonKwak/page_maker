# ============================================
# 네이버 스마트스토어 상세페이지 자동화 도구
# Synology NAS Docker 배포 설정
# ============================================

# API Keys (필수)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# PostgreSQL 데이터베이스 설정
# ============================================
POSTGRES_DB=detailpage
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_secure_password_here

# ============================================
# 포트 설정
# ============================================
# NAS에서 사용할 외부 포트 (다른 서비스와 충돌하지 않는 포트 선택)
HTTP_PORT=3500
HTTPS_PORT=3543

# ============================================
# 프론트엔드 설정
# ============================================
# Docker 내부 통신용 (수정 불필요)
NEXT_PUBLIC_API_URL=http://backend:8000

# ============================================
# 시놀로지 NAS 배포 가이드
# ============================================
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. API 키와 비밀번호 설정
# 3. Container Manager에서 프로젝트 생성
#    - 프로젝트 이름: detailpage-maker
#    - 경로: /volume1/docker/detailpage-maker
# 4. docker-compose up -d 실행
# 5. http://NAS_IP:3500 접속
